
import React from 'react';
import {connect} from 'react-redux';

import {firstLoadRemarks} from '../actions.jsx';
///import request from '../requestHandler.jsx';

import Remarks from '../components/remarks.jsx';
/*
let req = request('GET', '/api/remarks', function (error, data) {
    if (error != undefined) {
        console.error(error);
    } else {
        console.log(data, 'erer');
        //dispatch(firstLoadRemarks(data));
    }
});
let mapDispatchToProps = req;

//let FirstLoadRemarks = ({dispatch}) => {

//};*/
export default function () {

};